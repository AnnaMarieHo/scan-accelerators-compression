[project]
name = "OLAP-Scans"
version = "0.1.0"
description = "Scan Acceleration and Compression"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.4",
    "pandas>=3.0.1",
]
